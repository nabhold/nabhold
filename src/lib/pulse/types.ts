import {z} from "zod";export const signalSchema=z.object({id:z.string(),title:z.string(),market:z.string(),direction:z.enum(["improving","stable","deteriorating"]),confidence:z.number().min(0).max(1),observedAt:z.string()});export const opportunitySchema=z.object({id:z.string(),title:z.string(),market:z.string(),score:z.number().min(0).max(100),rationale:z.string()});export type ExecutiveOverview={signals:z.infer<typeof signalSchema>[];opportunities:z.infer<typeof opportunitySchema>[];generatedAt:string};
