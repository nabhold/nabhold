import type {MetadataRoute} from "next";import {portfolio} from "@/features/portfolio/data";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL??"http://localhost:3000";return["","/about/group","/portfolio","/sectors","/insights",...portfolio.map(c=>`/portfolio/${c.slug}`)].map(path=>({url:`${base}${path}`,changeFrequency:"monthly"}))}
