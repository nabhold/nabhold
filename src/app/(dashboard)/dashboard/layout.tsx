import Link from "next/link";import {redirect} from "next/navigation";import {getSession} from "@/lib/auth/session";export const metadata={robots:{index:false,follow:false}};export const dynamic="force-dynamic";export default async function Layout({children}:{children:React.ReactNode}){const session=await getSession();if(!session)redirect("/sign-in");return <div className="min-h-screen bg-[#ece9e1]"><header className="bg-[var(--ink)] text-white"><div className="shell flex min-h-16 items-center justify-between"><Link href="/dashboard" className="font-serif text-xl">Nabhold Executive</Link><span className="text-xs text-white/65">{session.displayName}</span></div></header><div className="shell grid gap-8 py-8 md:grid-cols-[190px_1fr]"><aside><nav className="grid gap-2 text-sm font-semibold"><Link href="/dashboard">Overview</Link><span className="text-black/35">Pulse</span><span className="text-black/35">Portfolio</span><span className="text-black/35">Reports</span></nav></aside><main id="main">{children}</main></div></div>}
