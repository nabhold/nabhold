import {notFound} from "next/navigation";import {portfolio} from "@/features/portfolio/data";
export function generateStaticParams(){return portfolio.map(({slug})=>({slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=portfolio.find(x=>x.slug===slug);if(!c)notFound();return <article className="shell py-20"><p className="eyebrow">Portfolio company · {c.sector}</p><h1 className="display mt-5 text-6xl md:text-8xl">{c.name}</h1><div className="mt-12 grid gap-10 border-t border-[var(--line)] pt-10 md:grid-cols-2"><div><h2 className="font-serif text-2xl">Overview</h2><p className="mt-4 leading-7 text-[var(--muted)]">{c.summary}</p></div><div><h2 className="font-serif text-2xl">Markets</h2><p className="mt-4 text-[var(--muted)]">{c.markets}</p><a className="button mt-8" href={c.url} rel="noreferrer" target="_blank">Visit digital estate ↗</a></div></div></article>}
